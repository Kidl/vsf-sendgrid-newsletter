{
  "additionalProperties": true,
  "required": [
    "email",
    "country"
  ],
  "properties": {
    "email": {
      "type": "string",
      "format": "email"
    },
    "country": {
      "type": "string",
      "pattern": "^[A-Z]{3}$"
    }
  }
}
